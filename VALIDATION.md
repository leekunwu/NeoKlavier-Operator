# Validation — v0.1

2026-09-15. Chromium headless, 로컬 HTML, Playwright 및 Node 환경에서 확인했습니다.

## 통과한 자동 검증

- MIDI 파일 10개 입력 순서, 개별 손상 파일 격리
- PPQ 템포 변경을 포함한 다중 트랙 병합, running status, SMPTE
- 테스트 MIDI의 모든 절단 prefix를 손상 파일로 거부
- 재생 중 큐 위치 변경 후 동일 곡의 재생·하이라이트 유지
- 원본 70 × 150% = 105, 벨로시티 상한 127, Note On 0 보존
- Pause 위치 보존, Resume, Stop 및 sustain 해제 메시지
- 999초 프리롤 시작·시간 진행·Esc 취소 후 예약 재생 방지
- 속도 200%에서 원본 2초 파일 약 1초 재생
- 자동 진행 7초 갭 및 다음 곡 재생 (5ms 폴링 오차 허용)
- 페달 상승 엣지, 밟고 있는 동안 중복 실행 방지, 해제 후 재트리거
- CC 감도 경계값과 반전
- 가상 출력 장치 연결 해제 시 Error 및 활성 음 해제
- LocalStorage 설정·매핑 복원, MIDI 파일 미복원
- Web Audio 실제 AudioContext 초기화, 보이스 생성, Panic 시 보이스 해제
- 공연 화면 모드 전환
- 데스크톱 1512px 및 모바일 390px 배치, 가로 넘침 확인
- 브라우저 런타임 오류 없음

`tests/parser.cjs`는 Node만으로 실행할 수 있습니다. `tests/smoke.cjs`는 Playwright와 Chromium이 필요합니다. 추가 Web Audio·감도·반전 확인은 작업 중 별도 브라우저 검사로 실행했습니다.

## 현장 검증이 남은 항목

- 실제 MIDI 장치 권한 요청, 실물 풋 페달 Learn 및 디바운스 조정
- 실제 MIDI 포트 전송 지연·지터와 장시간/고밀도 파일 운용
- 실물 장치 분리 시 잔류 음 및 장치 자체 Panic
- GitHub Pages HTTPS 배포 후 대상 브라우저에서 전체 확인
- 기본 신스의 실제 청취 품질과 외부 음원의 채널·프로그램·컨트롤러 반응

자동 검증은 하드웨어·공연 환경 검증을 대체하지 않습니다. GitHub 저장소 생성 또는 배포는 실행하지 않았습니다.


# v0.2 추가 검증

- 기존 smoke 및 parser 테스트 재통과
- 입력칸·버튼·선택 메뉴·슬라이더·트랙 행에 포커스가 있는 상태에서 단축키 우선 실행
- 버튼 기본 동작과 전역 단축키의 이중 실행 없음
- 015 숫자 표시와 실제 값 일치
- 페달 슬라이더 4개, CC Learn, 처리값 실시간 표시
- 내장 SUITE 로드, 72px 미만 카드 높이, 고정 헤더 및 공연 화면 예외
- 데스크톱·390px 모바일 가로 넘침 없음
- OfflineAudioContext에서 Grand/Upright/EP 음 생성 및 서로 다른 출력 파형 확인
- 리버브 0%에서 노트 해제 뒤 잔향 없음, 100%에서 잔향 발생
- Panic 후 리버브 tail 구간 RMS = 0
- 음색·리버브 값 새로고침 후 복원

음색은 모델링 합성이며, 실물 피아노 녹음과의 청감 동등성을 검증한 것은 아닙니다. 외부 MIDI 하드웨어 현장 검증 범위는 v0.1과 같습니다.
